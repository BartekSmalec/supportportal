server:
  port: 5000
  error:
    whitelabel:
      enabled: false
#    path: '/error'

spring:
  #  mvc:
  #    throw-exception-if-no-handler-found: true
  #  resources:
  #    add-mappings: false
  config:
    import: optional:secrets-prod.yml

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/supportportal
    username: root
  jpa:
    show-sql: true
    hibernate:
      #      ddl-auto: create
      ddl-auto: update
    properties:
      dialect: org.hibernate.dialect.MySQL8Dialect

logging:
  level:
    root: debug
'


